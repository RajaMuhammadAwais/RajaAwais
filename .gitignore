# Node modules (installed dependencies)
node_modules/

# Build output (Vite, dist, and other build files)
dist/

# Public assets (static images, fonts, etc.)
public/

# Static assets folder (images, audio, etc.)
assets/

# Dependency and build-related files
package-lock.json
package.json

# Tailwind CSS files (generated files might not need to be tracked)
tailwind.config.js
postcss.config.js
vite.config.js

# CSS files (if generated by build tool)
App.css

# Specific files from your description:
# Ignore the .gitignore file itself (if accidentally added in the future)
.gitignore

# Log files
*.log

# System files
.DS_Store
Thumbs.db

# IDE files (if you're using VS Code, JetBrains, etc.)
.vscode/
.idea/
